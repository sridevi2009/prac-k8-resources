apiVersion: v1
kind: Pod
metadata:
  name: resource-restric
spec:
  containers:
  - name: container-1
    image: nginx
    ports:
    - containerPort: 80
    resources:
      requests:
        cpu: "100m"
        memory: "68mi"
      limits:
        cpu: "200m"
        memory: "128mi"   

        